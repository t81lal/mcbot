package org.topdank.mc.bot.impl.entity.impl.hanging;

import org.topdank.mc.bot.api.world.World;
import org.topdank.mc.bot.impl.entity.Entity;

public class HangingEntity extends Entity {

	// hanging location
	// direction
	
	public HangingEntity(World world, int id, float defaultWidth, float defaultHeight) {
		super(world, id, defaultWidth, defaultHeight);
	}
}